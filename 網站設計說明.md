# BIM 顧客服務管理師｜網站設計說明

本文件描述**基礎版**的產品定位、視覺系統與互動規範。線上練習：https://redyjohn.github.io/exam5/  
還原點：git tag `base`（閃卡隨內容長高、RWD 不溢出卡片）。

---

## 1. 產品定位

靜態單頁練習站，給考生複習 **BIM 顧客服務管理師 題庫（五）學霸解答 202410**。

設計目標：

- 安靜、可信，像訓練講義而不是行銷落地頁
- 先回想、再對答案，不要一進來就看到解答
- 手機與桌機都能完整讀完長答案，文字不可超出卡片框

品牌來源：台灣服務禮儀品質管理協會／興誠服務品質管理訓練機構。

---

## 2. 資訊架構

固定外框：頂欄品牌＋導覽、中間 `#app` 切換畫面、頁尾機構名稱。

| 畫面 | 用途 |
|---|---|
| 首頁 | 品牌、開始練習／閃卡、題數統計、十個單元卡片 |
| 練習 | 一題一頁：作答框 → 顯示參考答案 → 已掌握／再複習 |
| 閃卡 | 點擊翻面背誦；可依單元篩選、隨機出題 |
| 進度 | 已掌握比例與題目清單 |
| 案例 | 本版無案例資料，導覽「案例」隱藏 |

本版 **50 題、10 單元**：服務理念、表情聲音、儀態、容貌服裝、問候、溝通、電話接待、顧客抱怨、綜合、學霸。

---

## 3. 視覺系統

### 色彩

| Token | 色碼 | 用途 |
|---|---|---|
| `--ink` | `#14232e` | 內文 |
| `--ink-soft` | `#3d5160` | 次要說明 |
| `--paper` | `#f3f6f8` | 頁面底 |
| `--panel` | `#ffffff` | 卡片底 |
| `--line` | `#d4dde4` | 邊線 |
| `--teal` | `#0d6e6e` | 主色、主按鈕、品牌字 |
| `--teal-deep` | `#0a5454` | 主按鈕 hover |
| `--teal-soft` | `#e4f1f1` | 導覽選中、參考答案底 |
| `--navy` | `#1f3a52` | 標題 |
| `--amber` | `#b7791f` | 再複習按鈕 |
| 星星 | `#e8b923` | ★★／★★★ 重點標 |

背景：淡青／海軍藍光暈＋淺色網格，由上往下淡出。圓角 14px，輕陰影。

### 字體

- 內文：Noto Sans TC（後備 Microsoft JhengHei）
- 標題／品牌：Noto Serif TC（後備 PMingLiU）
- 行高約 1.65；閃卡答案 1.7

### 按鈕

- **主按鈕**：實心 teal，白字
- **次按鈕**：白底細框（ghost）
- **再複習**：amber
- 按下略縮小（0.98）

---

## 4. 畫面規範

### 首頁

- 大字 `BIM`（serif、teal）
- 標題「BIM 顧客服務管理師｜考試練習」
- 主行動：開始練習；次行動：閃卡背誦
- 三欄統計：問答題數、單元數、已掌握
- 單元卡：上方 3px 色條、題數、單元名、一句說明；hover 上移並改邊線為 teal

### 練習

- 頂部單元 chip（全部＋十單元），可換行
- 題目前可有黃色星星
- 先寫答案，再「顯示參考答案」
- 參考答案：左側 teal 色條、淺綠底
- 自我評分後自動下一題

### 閃卡（基礎版核心）

- 正反面以 **CSS grid 疊在同一格**，卡片高度 = 較高那一面，隨內容長高
- 點擊或空白鍵／Enter 翻面（rotateY 180°）
- **禁止**再把卡片寫死固定高度，或在 3D 翻面上做 `overflow` 裁切（文字會衝出框）
- 長文改由**頁面捲動**；底部操作列 sticky，避免翻到答案後找不到「下一張」
- 捲動卡片時位移超過約 8px 不翻面，避免誤觸
- 小螢幕（≤640px）：內距與字級縮小，主欄全寬減 1.25rem

### 進度

- 長條比例＋每題狀態：未練習／再複習／已掌握
- 可清除進度（localStorage `bim-practice-progress-v2`）

---

## 5. 重點星號

練習與閃卡題目前顯示黃色星星。

| 星數 | 意義 |
|---|---|
| ★★★ | 最高頻必背 |
| ★★ | 次要必背 |
| 無 | 一般題 |

同一題若同時符合兩級，維持 ★★★，不降級。資料欄位：`questions[].stars`（2 或 3）。

---

## 6. 內容與檔案

| 檔案 | 角色 |
|---|---|
| `index.html` | 外框、字體、導覽 |
| `styles.css` | 視覺與 RWD |
| `app.js` | 畫面切換、練習／閃卡／進度 |
| `data/questions.js` | `window.BIM_DATA`：meta、單元、題目、案例 |

題目物件：`id`、`category`、`type`（`qa`）、`q`、`a`、可選 `stars`。

---

## 7. 響應式

- 主欄：`min(980px, calc(100% - 2rem))`；640px 以下改 `calc(100% - 1.25rem)`
- Chip、導覽、按鈕皆可換行
- 單元卡 `auto-fit`、最小約 200px
- 640px 以下隱藏頂欄「顧客服務管理師」副標，統計改單欄
- 閃卡 `minmax(0, 1fr)`、`overflow-wrap: anywhere`，避免長中文撐破

驗證基準：服務理念篇第 6 張（八大單元）與學霸篇長解答，在約 360px 與桌機寬度都必須整段留在卡片內。

---

## 8. 技術約束

- 純靜態：HTML／CSS／JS，無打包，可直接 GitHub Pages
- 路徑相對根目錄（`/exam5/` 亦可）
- 無案例時不顯示案例入口
- 基礎版還原：`git checkout base -- index.html app.js styles.css data/questions.js`
